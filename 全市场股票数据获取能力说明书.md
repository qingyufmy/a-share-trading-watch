# 全市场股票数据获取能力说明书

生成日期：2026-06-02  
目标读者：接手本项目的另一个 agent 或工程实现者  
适用范围：A 股自选股订盘、盘前机会池、集合竞价候选、盘中全市场雷达、盘后复盘

> 这份说明书只描述“如何获取、解释、校验全市场股票数据”。它不是交易建议，也不是对任何非官方接口长期可用性的承诺。当前项目使用的是公开 Web 行情接口和本地同花顺上下文数据，适合做盘面扫描、候选排序和风控观察；若要逐笔成交、逐笔委托、委托队列、毫秒级延迟或生产级全市场行情，应接入正式授权的 Level-2/DataFeed 服务。

## 1. 一句话结论

当前系统的“全市场数据能力”分三层：

1. 全市场候选扫描：主要用东方财富网页行情接口 `qt/clist/get`，按成交额、涨幅、板块强弱拉取候选池。
2. 自选股和候选股实时确认：主要用腾讯快照 `qt.gtimg.cn` 和新浪分钟线 `CN_MinlineService.getMinlineData`。
3. 题材和语义上下文：主要用同花顺早盘、复盘、异动、资讯流、个股 timeline/news/noticeReport、社区、东方财富 F10 补充。

需要特别注意：

- 当前“全市场”不是交易所级完整行情源，而是基于东方财富 `clist` 的网页筛选能力。
- 当前“tick”不是逐笔成交 tick，而是轮询快照。`realtime_signal_engine.py` 的 `run_tick()` 表示每轮监控循环，不代表逐笔行情。
- 当前集合竞价数据是 09:25 后的结果快照和全市场候选扫描，不包含 09:15-09:25 每秒撮合预估、撤单明细、逐笔委托、订单队列。
- 如果要真正掌握竞价过程或逐笔成交，必须采购/接入 Level-2 或高频 DataFeed。

## 2. 当前代码里的数据源总表

| 数据类型 | 当前接口/来源 | 代码入口 | 能拿到什么 | 适合用途 | 主要边界 |
|---|---|---|---|---|---|
| 全市场股票快照/排行 | 东方财富 `qt/clist/get` | `intraday_report.fetch_market_scan_rows()` | 代码、名称、最新价、涨跌幅、成交量、成交额、最高/最低/开盘/昨收、换手、行业、概念 | 全市场扫描、成交额榜、涨幅榜、候选池 | 非官方网页接口；不保证全量实时；不含逐笔和委托队列 |
| 板块强弱 | 东方财富 `qt/clist/get`，`fs=m:90+t:2,m:90+t:3` | `after_close_report.fetch_boards()` | 行业/概念板块代码、名称、价格/点位、涨跌幅、主力净流入字段 | 判断题材情绪、生成主题关键词 | 板块分类来自东财口径，和同花顺/申万不完全一致 |
| 指数快照 | 东方财富 `qt/ulist.np/get`，失败后腾讯 | `after_close_report.fetch_indexes()` | 上证、深成、创业板价格、涨跌幅、成交额、高低开昨 | 市场情绪 | 指数集固定为三大指数，非全指数库 |
| 自选股快照 | 腾讯 `https://qt.gtimg.cn/q=...` | `after_close_report.parse_tencent_quotes()` | 名称、现价/收盘、昨收、开盘、成交量、时间、涨跌、涨跌幅、最高、最低、成交额、换手 | 自选股订盘、集合竞价后高低开、盘中实时监控 | 需要已有代码列表；不是全市场发现接口 |
| 分钟线 | 新浪 `CN_MinlineService.getMinlineData` | `after_close_report.fetch_minline()` | 1 分钟价格、分钟量、累计量、均价 | VWAP、OR5/OR15、30/60 分钟聚合、开盘后承接 | 不是逐笔；通常是分钟级；不能复原委托/撤单 |
| 日线历史 | 搜狐 `hisHq` | `after_close_report.fetch_sohu_daily()` | 日开高低收、涨跌、涨跌幅、成交量、成交额、换手 | MA、MACD、趋势、压力支撑 | 当前代码从 `20250101` 起取，不是完整历史 |
| 同花顺自选股 | 本机同花顺 Mac 容器文件 | `after_close_report.read_watchlist()` | 自选股代码和市场 id | 读取用户真实“我的股票” | 路径和 plist 结构依赖本机同花顺版本/账号 |
| 同花顺个股资讯 | `flow.10jqka.com.cn/pc/timeline/...` | `after_close_report.fetch_ths_news()` | 个股新闻、公告/研报 timeline | 催化、公告、情绪解释 | 非行情；部分字段/权限可能变化 |
| 同花顺社区 | `t.10jqka.com.cn/guba/{code}/` + POST `newcircle/post/getPostList/` | `after_close_report.fetch_ths_news()` | 社区帖子标题/内容片段 | 情绪温度计 | 需要本地 cookie；不能当事实源 |
| 同花顺盘前/复盘/异动 | `stock.10jqka.com.cn/zaopan/`、`fupan/`、`yuanchuang.10jqka.com.cn/zhangting/`、`stock.10jqka.com.cn/bidu/` | `premarket_report.fetch_ths_discover_focus()` | 早盘必读、复盘主题、异动观察、停复牌、资金流、涨停原因等 | 生成主题关键词，解释题材 | HTML/GBK 页面，解析较脆 |
| 东方财富 F10 | `emweb.securities.eastmoney.com/PC_HSF10/...` | `after_close_report.fetch_f10()` | 公司概况、概念、股东户数 | 个股右侧资料补充 | 非实时行情；字段结构可能变 |

## 3. 代码入口地图

新 agent 应先记住这些函数：

| 文件 | 函数 | 作用 |
|---|---|---|
| `after_close_report.py` | `fetch_json()` / `fetch_text()` / `fetch_bytes()` | 通用 HTTP 获取，带 `User-Agent` 和重试 |
| `after_close_report.py` | `read_watchlist()` | 从环境变量、本地同花顺自选、fallback 文件读取股票池 |
| `after_close_report.py` | `parse_tencent_quotes(codes)` | 批量拉自选股/候选股快照 |
| `after_close_report.py` | `fetch_indexes()` | 拉三大指数 |
| `after_close_report.py` | `fetch_boards()` | 拉行业/概念板块强弱 |
| `after_close_report.py` | `fetch_sohu_daily(code)` | 拉日线历史 |
| `after_close_report.py` | `fetch_minline(code)` | 拉新浪 1 分钟线 |
| `after_close_report.py` | `fetch_ths_news(code, market)` | 拉同花顺个股资讯、公告、社区 |
| `after_close_report.py` | `fetch_f10(code)` | 拉东方财富 F10 资料 |
| `intraday_report.py` | `fetch_market_scan_rows(fid, size)` | 东方财富全市场股票扫描 |
| `intraday_report.py` | `em_row_to_quote(row)` | 将东财字段标准化成项目 quote |
| `intraday_report.py` | `select_radar_candidates(existing_codes, boards)` | 盘中全市场雷达筛选 |
| `premarket_report.py` | `select_premarket_opportunities(...)` | 盘前全市场机会池 |
| `auction_report.py` | `fetch_auction_candidates(...)` | 09:25 集合竞价后全市场候选 |
| `realtime_signal_engine.py` | `run_tick(...)` | 盘中轮询快照、分钟线、雷达和信号 |

## 4. 东方财富全市场股票扫描

### 4.1 接口性质

当前项目用的是东方财富网页行情接口：

```text
https://push2delay.eastmoney.com/api/qt/clist/get?
https://82.push2.eastmoney.com/api/qt/clist/get?
https://push2.eastmoney.com/api/qt/clist/get?
```

这是网页端公开接口，不是正式授权行情数据接口。稳定性中等，必须做 host fallback。2026-06-02 本地实测中，`push2.eastmoney.com` 曾返回 502，而 `push2delay.eastmoney.com` 正常返回，所以不要只依赖一个 host。

### 4.2 股票扫描参数

当前股票扫描参数：

```text
pn=1
pz={size}
po=1
np=1
ut=bd1d9ddb04089700cf9c27f6f7426281
fltt=2
invt=2
fid={fid}
fs=m:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23
fields=f12,f14,f2,f3,f4,f5,f6,f15,f16,f17,f18,f8,f10,f100,f102,f103
```

关键含义：

| 参数 | 当前用法 | 说明 |
|---|---|---|
| `pn` | `1` | 页码。当前只取第一页；如果要全量股票，应分页 |
| `pz` | 盘中 100-180，盘前 160-220，竞价 180-260 | 每页数量 |
| `fid` | `f6` 或 `f3` | 排序字段。`f6` 成交额，`f3` 涨跌幅 |
| `po` | `1` | 当前用于降序排行 |
| `fs` | `m:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23` | 当前项目的沪深 A 股、科创板、创业板网页筛选口径 |
| `fields` | 见下表 | 返回字段列表 |

字段映射：

| 字段 | 标准化字段 | 说明 |
|---|---|---|
| `f12` | `code` | 股票代码 |
| `f14` | `name` | 股票名称 |
| `f2` | `close` | 最新价/当前价；收盘后可视作收盘价 |
| `f18` | `prev_close` | 昨收 |
| `f17` | `open` | 今日开盘价；09:25 后可用于集合竞价高低开判断 |
| `f15` | `high` | 当日最高 |
| `f16` | `low` | 当日最低 |
| `f5` | `volume_lot` | 成交量，通常是手 |
| `f6` | `amount_yuan` | 成交额，单位通常按元处理 |
| `f4` | `change` | 涨跌额 |
| `f3` | `pct` | 涨跌幅，百分数数值 |
| `f8` | `turnover` | 换手率 |
| `f10` | 未核心使用 | 常见口径为量比，但当前系统不依赖 |
| `f100` | `industry` | 行业/所属板块文字 |
| `f102` | 未核心使用 | 常见口径为地域/扩展分类，当前系统不依赖 |
| `f103` | `concepts` | 概念文字 |

标准化逻辑在 `intraday_report.em_row_to_quote()`：

```python
{
    "name": row["f14"],
    "code": row["f12"],
    "close": float(row["f2"]),
    "prev_close": float(row["f18"]),
    "open": float(row["f17"]),
    "volume_lot": float(row["f5"]),
    "change": float(row["f4"]),
    "pct": float(row["f3"]),
    "high": float(row["f15"]),
    "low": float(row["f16"]),
    "amount_wan": float(row["f6"]) / 10000,
    "turnover": float(row["f8"]),
    "industry": row["f100"],
    "concepts": row["f103"],
}
```

### 4.3 当前不是完整全量，而是“全市场候选扫描”

当前项目里的“全市场雷达”不是把 5000 多只 A 股全部下载并逐只计算，而是：

1. 按成交额排序取一批。
2. 按涨幅排序取一批。
3. 合并去重。
4. 用板块/题材/成交额/日内位置筛选。

当前参数：

| 场景 | 成交额榜 | 涨幅榜 | 阈值 | 输出 |
|---|---:|---:|---:|---:|
| 盘中雷达 | `fid=f6, pz=180` | `fid=f3, pz=100` | `score >= 4.6` | 最多 6 只 |
| 盘前机会池 | `fid=f6, pz=220` | `fid=f3, pz=160` | `score >= 5.6` | 最多 8 只 |
| 09:25 竞价候选 | `fid=f6, pz=260` | `fid=f3, pz=180` | `score >= 6.8` 且日线技术过滤通过 | 最多 10 只 |

这套做法的优势是快、稳定、适合盘中；缺点是会漏掉：

- 成交额不靠前、涨幅也不靠前但形态刚启动的票。
- 低波动埋伏票。
- 某些冷门板块的首批异动。
- 北交所、B 股、ETF、可转债等当前未纳入口径的品种。

### 4.4 如果要真正“全量 A 股列表”

可以在东方财富 `clist/get` 上做分页，而不是只取第一页：

```python
def fetch_all_a_share_rows(fetch_json):
    hosts = [
        "https://push2delay.eastmoney.com/api/qt/clist/get?",
        "https://82.push2.eastmoney.com/api/qt/clist/get?",
        "https://push2.eastmoney.com/api/qt/clist/get?",
    ]
    fields = "f12,f14,f2,f3,f4,f5,f6,f15,f16,f17,f18,f8,f10,f100,f102,f103"
    fs = "m:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23"
    rows = []
    page = 1
    page_size = 100
    while True:
        query = (
            f"pn={page}&pz={page_size}&po=1&np=1"
            "&ut=bd1d9ddb04089700cf9c27f6f7426281&fltt=2&invt=2"
            f"&fid=f12&fs={fs}&fields={fields}"
        )
        data = None
        for host in hosts:
            try:
                data = fetch_json(host + query, headers={"Referer": "https://quote.eastmoney.com/center/gridlist.html"}, timeout=12)
                break
            except Exception:
                continue
        diff = ((data or {}).get("data") or {}).get("diff") or []
        if not diff:
            break
        rows.extend(diff)
        if len(diff) < page_size:
            break
        page += 1
    return rows
```

工程建议：

- 全量下载用于盘前/盘后批处理，不建议每 5 秒全量下载。
- 盘中雷达仍建议用成交额榜和涨幅榜，因为目标是找“已经被市场确认的强势替代机会”。
- 如果做全量下载，必须缓存、限速、失败重试，并记录 `generated_at`。
- 不要默认 `pz=5000` 一次拉完。网页接口可能变慢、失败或被限流；分页更稳。

## 5. 东方财富板块强弱

当前接口仍是 `qt/clist/get`，但 `fs` 改为板块口径：

```text
pn=1
pz=120
po=1
np=1
ut=bd1d9ddb04089700cf9c27f6f7426281
fltt=2
invt=2
fid=f3
fs=m:90+t:2,m:90+t:3
fields=f12,f14,f2,f3,f62
```

字段：

| 字段 | 说明 |
|---|---|
| `f12` | 板块代码 |
| `f14` | 板块名称 |
| `f2` | 板块价格/点位 |
| `f3` | 板块涨跌幅 |
| `f62` | 常见口径为主力净流入相关字段；当前报告没有核心依赖 |

当前 `fetch_boards()` 返回：

```python
return boards[:15], keep
```

其中：

- `boards[:15]` 是涨幅前 15 的行业/概念板块。
- `keep` 是项目手动关注关键词的板块缓存，关键词包括 `PCB`、`印制电路板`、`CPO`、`光通信`、`算力`、`数据中心`、`半导体`、`先进封装`、`培育钻石`、`玻璃`、`玻纤`、`元件`、`军工`、`低空`、`光刻`、`MLCC`、`被动元件`。

边界：

- 东财板块分类和同花顺、申万、中信行业不完全一致。
- `f14` 是文本匹配来源，不能当作严格行业标准。
- 盘中强势板块可能快速变化，应在报告中标明生成时间。

## 6. 腾讯快照：自选股和候选股确认

接口：

```text
https://qt.gtimg.cn/q=sh600000,sz000001,sh000001,sz399001,sz399006
```

当前代码通过 `stock_symbol(code)` 拼前缀：

```python
("sh" if code.startswith("6") else "sz") + code
```

当前解析字段来自 `~` 分隔文本：

| 位置 | 标准化字段 | 说明 |
|---:|---|---|
| `parts[1]` | `name` | 名称 |
| `parts[2]` | `code` | 代码 |
| `parts[3]` | `close` | 最新价/当前价 |
| `parts[4]` | `prev_close` | 昨收 |
| `parts[5]` | `open` | 今日开盘 |
| `parts[6]` | `volume_lot` | 成交量 |
| `parts[30]` | `datetime` | 行情时间 |
| `parts[31]` | `change` | 涨跌额 |
| `parts[32]` | `pct` | 涨跌幅 |
| `parts[33]` | `high` | 最高 |
| `parts[34]` | `low` | 最低 |
| `parts[37]` | `amount_wan` | 成交额，当前代码按万元使用 |
| `parts[38]` | `turnover` | 换手 |

适合：

- 自选股监控。
- 集合竞价后观察高低开。
- 盘中每 5 秒轮询快照。
- 候选股进入模拟盘后跟踪现价。

不适合：

- 不适合做全市场发现，因为它需要预先知道代码列表。
- 不适合当逐笔成交，因为它是快照。
- 不适合严格重放盘中交易过程。

工程注意：

- 文本编码是 GBK。
- 批量代码过多时要分批，避免 URL 过长。
- 字段位置是网页接口经验值，若接口格式变化，必须用样本行重新校准。

## 7. 新浪分钟线：VWAP 和开盘承接

接口：

```text
https://quotes.sina.cn/cn/api/openapi.php/CN_MinlineService.getMinlineData?symbol=sh600000&dpc=1
```

当前返回字段：

| 字段 | 说明 |
|---|---|
| `m` | 时间，如 `09:31:00` |
| `p` | 分钟价格 |
| `v` | 分钟成交量 |
| `avg_p` | 均价，可用作 VWAP 近似 |
| `tot_v` | 累计成交量 |

当前用途：

- `intraday_report.minline_stats()`：计算 VWAP、开盘区间高低点、近 30/60 分钟高低点、量能趋势。
- `after_close_report.aggregate_intraday()`：聚合 30 分钟、60 分钟。
- `realtime_signal_engine.minute_features()`：计算 OR5/OR15、ATR1m/5m、VWAP、分钟线延迟等。

边界：

- 这是分钟线，不是逐笔成交。
- 只能在有代码列表时逐只拉，不能直接全市场推送。
- 盘中刷新频率不宜过高。当前实时引擎默认每 20 秒刷新一次分钟线。
- 分钟线可用于“开盘后承接确认”，但不能还原 09:15-09:25 的竞价撤单过程。

## 8. 搜狐日线：技术结构和历史复盘

接口：

```text
https://q.stock.sohu.com/hisHq?code=cn_600000&start=20250101&end=YYYYMMDD&stat=1&order=D&period=d&callback=historySearchHandler&rt=jsonp
```

当前字段：

| 字段 | 说明 |
|---|---|
| 日期 | 交易日 |
| 开盘、收盘、涨跌、涨跌幅、最低、最高 | 日 K 基础字段 |
| 成交量、成交额、换手 | 量价字段 |

当前用途：

- 计算 MA5/10/20/30/60。
- 计算 MACD。
- 识别近期高低点、压力支撑。
- 竞价候选二次过滤：趋势是否还在、是否乖离过大、是否高位放量长阴。

边界：

- 当前代码 `start=20250101`，所以长期周线/月线不足。
- 搜狐接口是 JSONP，需要剥离 callback。
- 复权口径当前没有显式处理；若做严肃回测，必须统一前复权/后复权/不复权口径。

## 9. 同花顺数据：语义上下文，不是全市场行情主源

### 9.1 本地自选股

当前默认文件：

```text
/Users/tonyyu/Library/Containers/cn.com.10jqka.macstock/Data/Documents/cloud_store/blockstock_438990899/public/blockstock_438990899__public__CCk=.dat
```

读取优先级：

1. 环境变量 `A_SHARE_WATCHLIST_CODES`。
2. 同花顺本地自选 plist。
3. `watchlist_fallback.json`。

市场 id 当前经验映射：

| 代码 | 同花顺 market id |
|---|---|
| 以 `6` 开头 | `17` |
| 其他深市/创业板常见代码 | `33` |

边界：

- 本地文件结构依赖同花顺 Mac 版和账号，不能写入，只能只读解析。
- 当前 `read_watchlist()` 正则主要匹配 `[036]\d{5}`，不覆盖北交所等特殊代码。

### 9.2 个股资讯和公告

接口：

```text
https://flow.10jqka.com.cn/pc/timeline/v1/base/news/{market}/{code}/first/1
https://flow.10jqka.com.cn/pc/timeline/v1/base/noticeReport/{market}/{code}/first/1
```

用途：

- 个股近期新闻。
- 公告、研报、融资相关标题。
- 情绪和催化解释。

边界：

- 这是语义数据，不是行情数据。
- 某些字段可能缺失，不能用它决定买卖价格。

### 9.3 社区情绪

流程：

1. 打开 `https://t.10jqka.com.cn/guba/{code}/`。
2. 从 HTML 中找 `data-fid`。
3. POST：

```text
https://t.10jqka.com.cn/newcircle/post/getPostList/
```

payload：

```text
type=1&fid={fid}&first=1
```

headers：

```text
X-Requested-With: XMLHttpRequest
Referer: https://t.10jqka.com.cn/guba/{code}/
Cookie: 从本机同花顺 Cookies.binarycookies 解析出的 10jqka cookie
```

使用原则：

- 只做情绪温度计。
- `type=1` 才作为社区帖子来源。
- 如果帖子为空，标为 `冷静/低关注（样本不足）`。
- 不使用社区传言作为事实催化。
- 不打印、不泄露 cookie。

### 9.4 盘前/复盘/异动页面

当前页面：

```text
https://stock.10jqka.com.cn/zaopan/
https://stock.10jqka.com.cn/fupan/
https://yuanchuang.10jqka.com.cn/zhangting/
https://stock.10jqka.com.cn/bidu/
https://stock.10jqka.com.cn/thsgd/ywjh.js
```

用途：

- 提取早盘必读、昨日回顾、重大新闻、公司公告、券商观点、今日财经数据与事件。
- 提取复盘综合描述、主流看点、活跃个股脉络。
- 提取异动观察、涨停原因、受影响个股。
- 提取右侧特色模块，如停复牌、大宗交易、公告日历、资金流向、涨停股原因。
- 与东财板块名称和个股行业/概念匹配，生成主题关键词。

边界：

- 多数是 HTML/GBK 页面，结构变化会影响解析。
- 不能替代行情源。
- 更适合做“题材解释”和“优先级调整”。

## 10. 东方财富 F10：公司资料补充

当前接口：

```text
https://emweb.securities.eastmoney.com/PC_HSF10/CompanySurvey/PageAjax?code=SH600000
https://emweb.securities.eastmoney.com/PC_HSF10/CoreConception/PageAjax?code=SH600000
https://emweb.securities.eastmoney.com/PC_HSF10/ShareholderResearch/PageAjax?code=SH600000
```

当前拿：

- 公司行业、主营/简介。
- 概念列表。
- 股东户数、变化比例、集中度描述。

用途：

- 个股右侧资料。
- 题材匹配的二次确认。
- 筹码集中/分散的背景解释。

边界：

- 更新频率低于行情。
- 不适合盘中实时交易。
- 字段结构可能调整，应容错。

## 11. 集合竞价数据：当前能拿多细

### 11.1 当前实际能力

当前 `auction_report.py` 在 09:25-09:30 运行，做的是“集合竞价结束后的快照判断”：

1. 读取 08:30 盘前关键位。
2. 用腾讯快照读取自选股 09:25 后价格、开盘价、昨收、成交额。
3. 用东方财富 `clist/get` 取全市场成交额榜和涨幅榜。
4. 用同花顺盘前主题 + 东财板块做题材匹配。
5. 用搜狐日线做技术过滤。
6. 输出全市场竞价候选，并写入 `data/auction_candidates/auction_candidates_YYYYMMDD.json`。

当前能判断：

- 个股高开/低开幅度：`(open - prev_close) / prev_close`。
- 竞价后成交额强弱：快照成交额。
- 是否属于强板块：行业字段是否命中板块榜文本。
- 是否命中盘前主题：行业/概念/名称与主题关键词匹配。
- 日线是否过热、趋势是否还在。
- 09:30 后是否站稳触发价、开盘价、VWAP、OR5/OR15。

### 11.2 当前拿不到什么

当前拿不到：

- 09:15-09:25 每一秒/每一笔的虚拟撮合价变化。
- 09:15-09:20 可撤单阶段的撤单明细。
- 09:20-09:25 不可撤单阶段的真实委托队列变化。
- 委买委卖总量、加权委买/委卖价、十档盘口。
- 逐笔委托。
- 逐笔成交。
- 订单号、买卖方订单关联。
- 集合竞价订单不平衡量。

所以当前竞价报告的准确说法应是：

> 基于 09:25 后公开快照的集合竞价结果筛选。

不应该说：

> 已经掌握集合竞价全过程、逐笔委托、撤单行为或主力排单。

### 11.3 可以扩展到的“公开快照竞价采样”

如果不接 Level-2，也可以做一个更细的 09:15-09:25 采样器：

```python
def collect_auction_snapshots(codes, now, fetch_quotes, output_jsonl):
    # 09:15-09:25 每 3-5 秒运行一次
    quotes = fetch_quotes(codes)
    for code, q in quotes.items():
        output_jsonl.write({
            "ts": now.strftime("%Y-%m-%d %H:%M:%S"),
            "code": code,
            "name": q["name"],
            "latest": q["close"],
            "open": q["open"],
            "prev_close": q["prev_close"],
            "amount_wan": q["amount_wan"],
            "pct": q["pct"],
            "high": q["high"],
            "low": q["low"],
        })
```

可计算的代理指标：

- 竞价价格稳定性：09:20 后价格是否稳定。
- 竞价额变化：采样成交额是否持续抬升。
- 高开质量：高开幅度是否温和，是否过高。
- 竞价后承接验证：09:30-09:35 是否站稳开盘价/VWAP/OR5。

但必须标注为“快照采样代理指标”，不是逐笔竞价数据。

当前项目已经落地这个采样器：

```text
auction_path_collector.py
```

默认行为：

- 工作日 09:15:00 到 09:24:56 运行。
- 自选股快照默认每 3 秒采样一次。
- 东方财富全市场成交额榜/涨幅榜默认每 15 秒采样一次。
- 09:25 的最终集合竞价结果仍由 `auction_report.py` 捕捉，避免采样器占住原有竞价报告。

输出位置：

```text
data/auction_path/auction_path_YYYYMMDD.jsonl
data/auction_path/auction_path_summary_YYYYMMDD.json
data/auction_path/auction_path_latest.json
```

手动测试：

```bash
python3 auction_path_collector.py --once --force
```

只采自选股、不采全市场榜单：

```bash
python3 auction_path_collector.py --once --force --watchlist-only
```

接入调度：

- `run_trading_job.py` 已支持 `--mode auctionpath`。
- `install_launch_agent.py` 已把 09:15 加入自动调度，并会复制 `auction_path_collector.py` 到运行目录。
- 重新安装 launchd 后，09:15 自动启动竞价路径采样，09:25 自动生成原来的集合竞价报告。

采样器能沉淀的代理指标：

- 自选股 09:15-09:24:56 的可见价格、涨幅、成交额变化。
- 全市场成交额榜/涨幅榜出现次数、首次出现时间、最后出现时间、最好排名。
- 竞价阶段强势候选是否持续在榜，还是一闪而过。
- 09:25 报告和 09:30 后承接复盘可以回看这些轨迹。

### 11.4 如果要真实集合竞价微观数据

需要正式授权 Level-2 或高频 DataFeed。公开资料显示：

- 上证所信息 Level-2 在基础行情上增加盘口订单、逐笔成交、成交与订单关联等数据，并提供 10 档价格、数量、委托笔数以及第一档前 50 笔订单量。
- 上证所行情历史数据产品包含快照、逐笔成交、日 K、分钟 K 和集合竞价。
- 同花顺 DataFeed Level-2 属于收费高频行情推送，包含逐笔成交、逐笔委托、委托队列、极速盘口；Level-1 是 5 档、约 3 秒/笔，Level-2 是 10 档且支持逐笔。

这些能力不应通过未授权网页抓取替代。

## 12. Tick 数据：必须先定义清楚

中文交易语境里至少有三种“tick”：

| 名称 | 含义 | 当前项目是否具备 |
|---|---|---|
| 轮询 tick | 程序每隔几秒拉一次快照 | 具备。`realtime_signal_engine.py` 默认 5 秒一次 |
| 分笔/快照 tick | 某行情源按 3 秒或更短频率给快照 | 部分具备。腾讯/东财可轮询，非正式推送 |
| 逐笔成交/逐笔委托 | 交易所 Level-2 微观数据 | 当前不具备 |

当前系统不要使用“tick 数据”这个词泛指逐笔成交。建议统一写：

- “快照轮询”
- “分钟线”
- “Level-2 逐笔成交”
- “Level-2 逐笔委托”
- “委托队列”

## 13. 稳定性评估

| 来源 | 稳定性 | 原因 | 必要防护 |
|---|---|---|---|
| 东方财富 `clist/get` | 中 | 网页接口，主域可能 502，字段可能变化 | 三 host fallback、timeout、headers、字段缺失容错、缓存 |
| 腾讯 `qt.gtimg.cn` | 中高 | 老牌网页快照接口，格式稳定但非正式合同 | GBK 解码、分批请求、字段长度校验 |
| 新浪分钟线 | 中 | 接口可用性较好，但不是正式 SLA | 空数据容错、分钟延迟检查、不要过高频 |
| 搜狐日线 | 中 | JSONP 老接口，历史数据适合补充 | callback 校验、复权口径说明、失败降级 |
| 同花顺 HTML 页面 | 中低 | 页面结构容易变，GBK/HTML 解析脆 | 只用于语义增强，失败不阻塞行情 |
| 同花顺社区 | 中低 | 需要 cookie，可能未登录或空样本 | 不泄露 cookie，未登录则标不可用 |
| 东方财富 F10 | 中 | PC F10 接口较稳定但非实时 | 字段容错，作为补充资料 |
| 正式 Level-2/DataFeed | 高 | 合同化、文档化、推送式 | 需要授权、费用、鉴权、带宽和存储 |

## 14. 盘中实时引擎当前节奏

`realtime_signal_engine.py` 默认参数：

```text
--interval 5.0        # 快照轮询间隔，秒
--bar-refresh 20.0   # 分钟线刷新间隔，秒
--radar-refresh 120  # 全市场雷达刷新间隔，秒
```

实际流程：

1. 读取盘前关键位。
2. 腾讯快照拉自选股现价。
3. 每 20 秒刷新自选股分钟线。
4. 每 120 秒刷新全市场雷达候选。
5. 对候选补拉新浪分钟线。
6. 生成信号、写 Dashboard、推飞书、记录模拟盘。

这套节奏适合人工订盘和模拟盘跟踪，不适合高频交易。

## 15. 数据边界和覆盖范围

当前默认覆盖：

- 沪市主板常见 `6xxxxx`。
- 科创板 `688xxx` 等，因为东财 `fs` 包含 `m:0+t:80`。
- 深市主板/中小板常见 `0xxxxx`。
- 创业板 `30xxxx`，因为东财 `fs` 包含 `m:1+t:23`。

当前没有明确覆盖：

- 北交所 `8xxxxx`、`4xxxxx`。
- B 股。
- ETF、LOF、可转债。
- 港股、美股作为全市场扫描。
- 严格申万/中信行业全量体系。
- Level-2 全市场逐笔。

如果要扩展这些品种，需要重新确认东方财富 `fs` 口径、腾讯/新浪代码前缀、历史 K 线接口和交易规则。

## 16. 推荐给另一个 agent 的实现原则

### 16.1 不要把单一接口当真相

行情应分层确认：

1. 全市场发现：东方财富 `clist/get`。
2. 自选/候选现价确认：腾讯快照。
3. 开盘后承接：新浪分钟线。
4. 趋势结构：搜狐日线。
5. 题材解释：同花顺早盘/复盘/异动 + 东方财富 F10。

### 16.2 所有数据都要带时间

每份缓存都至少写：

```json
{
  "generated_at": "2026-06-02 09:25:10",
  "source": "eastmoney_clist",
  "query": "...",
  "rows": []
}
```

盘中报告必须标注生成时间，否则涨跌幅和板块强弱没有意义。

### 16.3 快照不能推导逐笔

快照能做：

- 当前强弱。
- 成交额辨识度。
- VWAP 附近承接。
- 板块和题材共振。

快照不能做：

- 主力真实排撤单。
- 大单逐笔扫货。
- 委托队列强弱。
- 订单不平衡。

### 16.4 失败时要降级

推荐降级顺序：

1. 东财主域失败，换 `push2delay` 和 `82.push2`。
2. 指数东财失败，换腾讯指数。
3. 分钟线失败，降低日内时机置信度，不编造 VWAP。
4. 同花顺页面失败，只丢语义解释，不影响行情主流程。
5. 社区失败，标记不可用，不用公告列表冒充社区。

### 16.5 不要泄露用户数据

- 不打印飞书 webhook。
- 不打印同花顺 cookie。
- 不改写同花顺本地文件。
- 不把用户自选股路径之外的文件当行情源乱扫。

## 17. 当前全市场候选评分框架

### 17.1 盘中雷达

候选来源：

```python
raw_rows = fetch_market_scan_rows("f6", 180) + fetch_market_scan_rows("f3", 100)
```

过滤：

- 排除自选股已有代码。
- 排除名称含 `ST`。
- 排除价格无效。
- 必须命中主题关键词或行业命中强势板块文本。
- `score >= 4.6`。

评分核心：

- 成交额越大，辨识度越高。
- 涨幅为正加分。
- 涨幅 0-7% 更优；大于 9% 反而防过热。
- 日内位置在高位加分，低位扣分。
- 当前价高于开盘价加分。
- 行业/概念/名称命中主题加分。
- 成交额低于 8 亿扣分。

### 17.2 盘前机会池

候选来源：

```python
raw_rows = fetch_market_scan_rows("f6", 220) + fetch_market_scan_rows("f3", 160)
```

主题来源更宽：

- 同花顺早盘。
- 同花顺复盘。
- 同花顺异动。
- 同花顺发现菜单资讯。
- 7x24。
- 板块前 12-15 名。
- 项目内政策主题词。

过滤：

- 排除已有自选股。
- 排除 ST。
- 必须命中主题或板块。
- `score >= 5.6`。
- 拉搜狐日线后，如果趋势优先级为 P0 风险则剔除。

### 17.3 集合竞价候选

候选来源：

```python
raw_rows = fetch_market_scan_rows("f6", 260) + fetch_market_scan_rows("f3", 180)
```

过滤更严格：

- `score >= 6.8`。
- 必须命中主题或板块。
- 必须通过 `technical_shape()` 日线结构过滤。

竞价评分关注：

- 竞价成交额。
- 高低开幅度，温和高开最优。
- 日内位置。
- 题材命中。
- 板块命中。
- 过高高开扣分。
- 低开太多扣分。
- 成交额低于 1.5 亿扣分。

## 18. 最小可用代码模板

下面是给新 agent 的最小实现骨架。正式写入项目时应复用现有函数，不要重复造轮子。

```python
import json
import time
import urllib.request

EASTMONEY_HOSTS = [
    "https://push2delay.eastmoney.com/api/qt/clist/get?",
    "https://82.push2.eastmoney.com/api/qt/clist/get?",
    "https://push2.eastmoney.com/api/qt/clist/get?",
]

HEADERS = {
    "User-Agent": "Mozilla/5.0",
    "Referer": "https://quote.eastmoney.com/center/gridlist.html",
}

def fetch_json(url, timeout=12):
    req = urllib.request.Request(url, headers=HEADERS)
    with urllib.request.urlopen(req, timeout=timeout) as resp:
        return json.loads(resp.read().decode("utf-8", "ignore"))

def fetch_eastmoney_clist(query):
    last_error = None
    for host in EASTMONEY_HOSTS:
        try:
            data = fetch_json(host + query)
            rows = ((data.get("data") or {}).get("diff") or [])
            if rows:
                return rows
        except Exception as exc:
            last_error = exc
            time.sleep(0.2)
    raise RuntimeError(f"eastmoney clist failed: {last_error}")

def fetch_top_amount_and_gainers():
    fields = "f12,f14,f2,f3,f4,f5,f6,f15,f16,f17,f18,f8,f10,f100,f102,f103"
    fs = "m:0+t:6,m:0+t:80,m:1+t:2,m:1+t:23"
    base = (
        "pn=1&po=1&np=1&ut=bd1d9ddb04089700cf9c27f6f7426281"
        "&fltt=2&invt=2"
        f"&fs={fs}&fields={fields}"
    )
    amount_rows = fetch_eastmoney_clist(base + "&pz=180&fid=f6")
    pct_rows = fetch_eastmoney_clist(base + "&pz=100&fid=f3")
    return amount_rows + pct_rows
```

## 19. 质量校验清单

新 agent 每次改取数逻辑后，至少检查：

- 东方财富股票扫描是否返回 `f12/f14/f2/f3/f6/f100/f103`。
- 东方财富板块扫描是否返回 `f14/f3`。
- 腾讯快照是否能解析出 `parts` 长度大于 40。
- 新浪分钟线是否至少返回 `m/p/v/avg_p/tot_v`。
- 日线是否按日期升序输出。
- `amount_wan` 单位是否一致：东财 `f6 / 10000`，腾讯 `parts[37]` 已按当前代码作为万元。
- 盘中报告是否写入生成时间。
- 接口失败是否有 fallback 或明确标不可用。
- 社区数据是否没有冒充公告/新闻。
- 没有把轮询快照称为逐笔 tick。

## 20. 何时应该升级到正式数据源

以下任一目标成立，就不应继续依赖公开网页接口：

- 需要真实逐笔成交。
- 需要逐笔委托。
- 需要十档盘口或委托队列。
- 需要集合竞价 09:15-09:25 全过程和撤单行为。
- 需要毫秒级延迟。
- 需要生产交易系统 SLA。
- 需要全市场实时推送而不是轮询。
- 需要商业化、对外展示或合规留痕。

可选方向：

- 上证所信息 Level-2/历史行情产品。
- 同花顺 DataFeed Level-1/Level-2。
- 券商或合规量化数据供应商提供的 Level-2 历史/实时数据。

## 21. 外部校验链接

这些链接用于理解能力边界，不是当前公开接口的授权依据：

- 上证所信息 Level-2 行情：`https://www.sseinfo.com/services/assortment/level2/`
- 上证所信息行情历史数据：`https://www.sseinfo.com/services/assortment/historical/`
- 同花顺 DataFeed 高频行情接口：`https://quant.10jqka.com.cn/view/dataplatform/detail/400`

其中，上证所信息页面说明 Level-2 增加盘口订单、逐笔成交、成交与订单关联等数据，并提供 10 档盘口和第一档前 50 笔订单量；行情历史数据页面说明历史数据产品包含快照、逐笔成交、日 K、分钟 K、集合竞价。同花顺 DataFeed 页面说明其 Level-2 是收费服务，包含逐笔成交、逐笔委托、委托队列和极速盘口。

## 22. 给新 agent 的最终行动准则

1. 先用东财 `clist/get` 找全市场候选，不要一开始逐只拉分钟线。
2. 候选出来后，用腾讯快照确认现价和高低开。
3. 需要日内执行时，再用新浪分钟线确认 VWAP、OR5/OR15 和量能。
4. 需要趋势过滤时，用搜狐日线重新计算 MA、MACD、近 20 日高低点。
5. 需要题材解释时，用同花顺早盘/复盘/异动和东财 F10，不要用社区传闻做事实。
6. 任何字段缺失或接口失败，都要明确写“不可用/数据不足”，不能补脑。
7. 当前系统没有 Level-2；看到“tick”先问清是轮询快照还是逐笔成交。
8. 集合竞价当前只做 09:25 后结果快照；若要过程数据，先做采样器，若要真实撤单/队列，接 Level-2。
9. 全市场扫描要限速、缓存、fallback，避免把公开网页接口当稳定数据中台。
10. 报告和缓存必须带时间戳、来源和字段口径。
