# Codex V2 策略系统 A 级迭代执行提示词

## 1. 使用方式

本文件用于把当前项目从 D 级影子系统迭代到 A 级可用系统。不要把全部改造放进一次超大任务。推荐在同一 Codex 任务中按阶段顺序执行，每完成一个阶段就检查验收结果，再进入下一阶段。

执行顺序：

1. 总控与基线冻结。
2. P0-1 逐股状态机和事件台账。
3. P0-2 分钟数据和已收盘 K 线正确性。
4. P0-3 评级、门控和动态预算。
5. P0-4 ExecutionContract 与模拟成交。
6. P0-5 持仓和退出闭环。
7. P1-1 历史回放、消融与系统评级。
8. P1-2 工作台和飞书体验。
9. 发布门禁与运行目录部署。

## 2. 全局总控提示词

```text
你正在维护本地 A 股盯盘项目：
/Users/tonyyu/Documents/Ai分析持仓股票订盘

运行目录：
/Users/tonyyu/Library/Application Support/a-share-trading-watch

目标不是增加交易次数，而是把系统从“扫描、生成报告”升级成可发现、可跟踪、可决策、可成交、可复盘、可运营的 A 级交易辅助系统。V2 只负责日内择时，盘前策略继续负责候选、仓位与风险预算。

必须遵守：
1. 先读取代码、测试、运行数据库和日志，禁止凭描述直接改参数。
2. 不删除、不清空、不移动、不重命名任何已有文件；不得使用 git clean、reset --hard、checkout --、rm 或批量覆盖。
3. 工作区已有大量未跟踪文件，全部视为用户基线；修改前后执行 git status --short，不能清理或恢复它们。
4. 手工编辑使用 apply_patch；改动必须小步、可测试、可解释。
5. 不得通过降低门槛或移除风险规则来人为制造信号。先修正确性和状态闭环，再用历史回放校准阈值。
6. V2 是策略条件的唯一裁决者。模拟盘不得重新计算量能、追价、RR、板块或技术形态后否决已通过的 V2 合约。
7. 执行层只检查交易时段、T+1、100 股、涨跌停、组合额度、执行区、滑点和流动性。
8. 数据缺失必须进入 DATA_BLOCKED，不能伪装成 V2_WAIT；任何未来 K 线或未收盘 K 线参与决策都属于 P0 故障。
9. 新仓和退出分离。持仓退出不能依赖盘前新仓资格。
10. 测试期间设置 A_SHARE_SKIP_FEISHU=1，不手工推送飞书，不自动下实盘订单。
11. 不直接修改运行目录。只有源代码测试、历史回放和发布门禁全部通过后，单独执行部署阶段。
12. 每个阶段必须输出：发现、设计、修改文件、测试结果、历史证据、剩余风险和下一阶段准入结论。

系统评级：
- S：90-100，生产成熟。
- A：80-89，可用。
- B：70-79，受控试运行。
- C：55-69，仅影子验证。
- D：低于 55，不可用于评价策略效果。

评级维度与权重：
- 数据正确性与时效 15%。
- 候选发现与覆盖 10%。
- 逐股状态跟踪 15%。
- 策略识别质量 15%。
- 执行与成交能力 15%。
- 持仓和风险管理 10%。
- 复盘和证据能力 15%。
- 工作台与消息体验 5%。

在当前阶段验收未通过时不要继续下一阶段，不要用“单元测试通过”替代端到端证据。
```

## 3. 阶段 0：基线冻结与问题台账

```text
执行阶段 0，只做基线审计和测试基座，不改变策略行为。

必须读取：
- realtime_signal_engine.py
- core/intraday_timing_v2.py
- core/local_market_data.py
- core/strategy_discipline.py
- core/sim_broker.py
- paper_trading.py
- after_close_report.py
- tests/ 下相关测试
- GPT_PRO_IMPLEMENTATION_REVIEW_20260601.md
- CODEX_V2策略系统A级迭代执行提示词.md

审计运行数据：
- data/runtime/signal_state.sqlite
- data/runtime/paper_trading.sqlite
- data/runtime/market_data.sqlite
- logs/trading_scheduler.log
- web_dashboard/data/runtime/signal_health.json

生成结构化基线报告 research/v2_a_grade_baseline.md，至少记录：
1. 2026-08-14 至最新交易日的 WAIT、NEAR、TRIGGER、订单、成交、退出数量。
2. 状态迁移矩阵。
3. 各阻断原因频率。
4. 盘前计划缺失率、分钟数据覆盖率、持仓识别率。
5. 模拟盘成交率、拒绝率、未成交原因。
6. 凯莱英、赤天化、多氟多、彤程新材四个案例的原始事件路径。
7. 当前八维评级和证据来源。

新增一个只读基线审计测试或脚本，禁止修改运行数据库。报告中的每个数字必须能由脚本重复计算。

完成定义：基线报告可重复生成；测试不推送飞书；没有策略行为变化；git status 无意外删除。
```

## 4. 阶段 P0-1：逐股状态机和事件台账

```text
执行 P0-1：把当前按 scenario 分裂的快照模型重构为逐股跟踪状态机。

目标状态：
DISCOVERED -> QUALIFIED -> SETUP_FORMING -> NEAR_TRIGGER -> TRIGGERED
-> ORDER_PENDING -> FILLED/PARTIAL/UNFILLED -> MANAGING -> REDUCE/EXIT -> CLOSED
旁路状态：DATA_BLOCKED、CANCELLED、EXPIRED。

设计要求：
1. 每个交易日每只股票只有一个活动 track_id；scenario 是属性，不是跟踪主键。
2. active state 表按 trading_date + symbol 唯一；append-only event 表记录每次有效变化。
3. 事件包含 old_state、new_state、grade、score、scenario、price、距离触发、主要缺口、数据时间、config_hash。
4. WAIT 内部也要识别评级跨档、缺口减少、进入触发距离、过热和取消，不能只在 scenario 改变时产生事件。
5. 保留旧 signals 表只读兼容或做非破坏迁移，不删除历史表。
6. fingerprint 不得只依赖 scenario；应覆盖状态、评级档位、触发距离档位和关键失效条件。
7. 普通价格抖动不产生事件；状态、评级或关键缺口变化才写入。

重点文件：realtime_signal_engine.py、数据库初始化/迁移模块、after_close_report.py、Dashboard 数据发布代码。

必须新增测试：
- V2_WAIT -> NEAR_TRIGGER -> TRIGGERED 是同一个 track_id。
- scenario 从 WAIT 变 E2 时不是 IDLE -> BUY_PROBE。
- 同状态价格抖动不重复写事件。
- 凯莱英回放能形成转强、过热、锁板不可买路径，即使最终无订单。
- 数据阻断恢复后可以从 DATA_BLOCKED 回到 SETUP_FORMING。

完成定义：目标股票均有连续路径；事件台账不再只有 IDLE->WAIT；旧报告仍可读取；全部测试通过。
```

## 5. 阶段 P0-2：分钟数据与已收盘 K 线正确性

```text
执行 P0-2：修复并证明 1m -> 5m/15m -> V2 的端到端数据链正确。

要求：
1. 腾讯为主源，新浪为备源；保留 source、bar_end、fetched_at、完整度和新鲜度。
2. 09:30-11:30、13:00-15:00 按 A 股交易分钟聚合，午休不能生成虚假 K 线。
3. 决策时点只能使用已经收盘的 5m/15m/120m K 线。
4. 5m 与 15m 必须来自同一时点可解释数据；禁止出现“5m=0、15m=64仍作为策略WAIT”的状态。
5. 数据不足时输出 DATA_BLOCKED、缺少数量、最后有效时间和预计恢复条件。
6. preopen quality 增加每个核心标的的历史准备度；盘中 health 增加已收盘 5m/15m 数量抽样。

新增不变量测试：
- 14:35 决策不能读取 15:00 K 线。
- 午休前后聚合数量正确。
- 241 根完整分钟线可生成预期 5m/15m 数量。
- 数据源返回旧序列时不覆盖新序列。
- 缺失一分钟时当前桶不冒充已收盘完整 K 线。
- 凯莱英 2026-08-25 数据重放不再出现 0/3、64/20。

完成定义：未来数据使用为 0；核心池分钟数据完整率至少 98%；数据故障与策略等待完全分离。
```

## 6. 阶段 P0-3：评级、门控和动态预算

```text
执行 P0-3：实现“Hard Veto + 七维评分 + 动态轮动预算”。

机会评分总分 100：
- 情绪 15：市场宽度、板块强度、持续性、龙头健康度。
- 筹码/位置 20：VWAP、成本区、支撑阻力、MID_AIR 风险。
- 大周期 20：120m 趋势、结构和空间。
- 中周期 15：15m Setup 完整度。
- 小周期 10：5m 收复、抬高低点、回踩质量。
- 量能 10：1m/5m、缩量回踩和放量恢复。
- 可执行性 10：价差、滑点、执行区、净 RR、涨跌停。

机会等级：A>=80、B=70-79、C=55-69、D<55，持仓风险用 R。Hard Veto 不能被评分救回。

Hard Veto 仅包括：关键数据缺失、120m BEAR/UNKNOWN、系统性红灯、GAP_FAILURE/DISTRIBUTION_SHOCK、锁死涨停、成本后 RR 不足、交易规则禁止。其余条件用于评分、排序或 NEAR_TRIGGER，不要全部硬 AND。

盘前授权拆分：
1. SYMBOL_PLAN：核心池逐股计划。
2. ROTATION_BUDGET：动态候选共享预算，配置总仓、单票首仓、每日新标的数量和风险上限。

动态候选满足 A 级、板块确认、数据健康、组合预算和 Hard Veto 全部通过时，可以继承 ROTATION_BUDGET，不因盘前未逐股列名永久失去资格。

必须新增测试：
- 缺少逐股计划但具有动态预算的 A 级候选可以进入 NEAR/TRIGGER。
- C/D 级不能下单。
- Hard Veto 无法被高评分救回。
- 只差一个非硬条件进入 NEAR_TRIGGER，并明确显示缺口。
- 行业暂缺不能误判成强板块，也不能静默通过。

完成定义：评分可解释、门控有唯一分类、动态候选具备真实升级路径，同时不放宽 Hard Veto。
```

## 7. 阶段 P0-4：ExecutionContract 与模拟成交

```text
执行 P0-4：建立 V2 唯一策略裁决和成本感知执行合约。

ExecutionContract 至少包含：
contract_id、strategy_version、config_hash、symbol、side、qty_cap、exec_low、exec_high、expiry、invalidation、target、expected_slippage、net_reward_risk、data_timestamp、strategy_evidence、portfolio_gate、exchange_gate。

规则：
1. V2 生成合约后，strategy_discipline 不得再次计算量能、追价、RR、板块或技术形态。
2. 执行层只检查时段、T+1、100 股、涨跌停、组合额度、执行区、滑点和流动性。
3. 执行带宽至少为 max(0.30*ATR5, 3*tick, 2*spread, P95_slippage)，参数应可配置并由回放校准。
4. 净 RR 使用预计成交价和成本计算，不使用理想信号价。
5. 确认后已离开执行区时生成 TRIGGERED_BUT_MISSED/UNFILLED，不得变回普通 WAIT。
6. 锁死涨停记录 LIMIT_LOCKED，不假设可以买到。
7. 信号失效位必须具有结构依据，并覆盖正常滑点和最小价格噪声。

必须新增测试：
- V2 通过后不会被第二套策略纪律拒绝。
- 执行区能覆盖合约声明的预计滑点。
- 多氟多案例不再出现口径冲突。
- 彤程新材案例要么给出可成交合约，要么在生成合约前判定不可执行。
- 重复 tick 不重复下单，过期合约不能成交。

完成定义：V2 合约与订单一一对应；策略二次拒绝为 0；非锁板有效合约的历史模拟成交率达到门禁目标。
```

## 8. 阶段 P0-5：持仓与退出闭环

```text
执行 P0-5：统一 PositionRegistry，并让退出链路独立工作。

字段至少包含：symbol、quantity、sellable、locked、avg_cost、lot_date、source、last_price、realized_pnl、unrealized_pnl。

要求：
1. 合并人工持仓、模拟持仓和未来实盘接口，不允许持仓股票在 V2 中显示 FLAT。
2. 每个 tick 优先评估持仓风险，再评估新仓。
3. REDUCE、TAKE_PROFIT、STRUCTURAL_EXIT 不依赖盘前开仓资格。
4. T+1 锁定部分记录风险但不能模拟卖出；可卖数量独立处理。
5. 退出也生成 ExecutionContract，并记录未成交原因。
6. 卖出后更新 lots、现金、持仓、已实现盈亏和状态机。

必须新增测试：
- 九芝堂、中国稀土模拟持仓被 V2 正确识别。
- 同日买入数量不可卖，下一交易日正确解锁。
- 持仓结构破坏能够生成退出合约。
- 空仓只记录风险观察，不产生卖单。
- 部分减仓后的数量、成本和盈亏一致。

完成定义：持仓误识别为 0；退出事件、订单、成交、账户快照可完整串联。
```

## 9. 阶段 P1-1：历史回放、消融与系统评级

```text
执行 P1-1：建立逐分钟、无未来函数的历史回放和发布评级。

首批回放区间为 2026-08-14 至 2026-08-26，重点案例：
- 凯莱英：强板块龙头转强、涨停锁板、不能追但必须完整跟踪。
- 赤天化：低开修复、冲高回落，检验修复型路线和假信号控制。
- 多氟多：验证 V2 与执行层口径统一。
- 彤程新材：验证执行带、滑点和失效距离。

输出指标：
1. DISCOVERED、QUALIFIED、NEAR、TRIGGER、ORDER、FILL、CANCEL、EXIT 漏斗。
2. 每类场景 1/3/5/15/30/60 分钟收益、MFE、MAE、失效命中和次日延续。
3. 成交后成本净收益和最大回撤。
4. 未成交原因、被拦截机会后续表现、TRIGGERED_BUT_MISSED 表现。
5. 候选相对板块、指数和未交易基线的表现。
6. 每个 Hard Veto/Required Setup 的消融结果。
7. 八维系统评级、证据和不达标项。

不得用全样本调参后在同一数据上宣称有效。至少拆分校准段和验证段；样本不足时明确标记“证据不足”，不能下盈利结论。

完成定义：回放可重复、无未来数据、参数变化有消融证据；评级低于 B 不允许进入部署阶段。
```

## 10. 阶段 P1-2：工作台与飞书体验

```text
执行 P1-2：把工作台从报告阅读器升级为操作台，并优化飞书消息。

首页固定优先级：
1. 立即处理：正式买卖合约、持仓结构退出。
2. 接近触发：A/B 级且只差少量非硬条件。
3. 持仓风险：NO_ADD、REDUCE、TAKE_PROFIT、EXIT。
4. 数据故障：DATA_BLOCKED、延迟、源不一致。
5. 普通观察：C 级及以下。

每张目标卡必须展示：当前状态、等级/总分、场景、距离触发、已通过条件、剩余缺口、执行区、失效位、目标/压力、净 RR、仓位预算、数据时间和最后状态变化时间。

飞书策略：
- 必须即时推：TRIGGERED、FILLED、PARTIAL、UNFILLED、CANCELLED、REDUCE、EXIT、P0 数据故障。
- 只进工作台：普通 WAIT、价格小幅波动、未跨级评分变化。
- 整点摘要：只列状态变化、最近触发前 5 名、持仓风险和数据健康，不重复几十条 WAIT。
- P0 恶化可重推；其他事件按 track_id + state + grade_bucket + trigger_distance_bucket 去重。

必须用 Playwright 检查桌面和移动视口，确认无空字段、无文本重叠、卡片层级清晰。测试期间不得真实推送飞书，只验证卡片 JSON 和渲染结果。

完成定义：用户打开第一页即可知道“现在做什么、最接近什么、为什么不能做”；重复消息率低于 5%。
```

## 11. 发布门禁与部署提示词

```text
执行最终发布审计。不要默认部署，先生成明确的 PASS/FAIL 报告。

门禁：
1. 数据完整率 >=98%，未来 K 线使用为 0。
2. 逐股状态变化记录率 100%，不存在 scenario 切换后从 IDLE 重启。
3. 模拟/人工持仓误识别为 FLAT 的数量为 0。
4. V2 合约生成后的策略二次拒绝为 0。
5. 正常非锁板有效合约历史模拟成交率 >=70%，未成交均有微观结构证据。
6. 所有 Hard Veto、T+1、涨跌停和仓位上限测试通过。
7. 回放无未来数据，校准段与验证段分离。
8. 工作台桌面/移动截图通过，飞书测试未真实发送。
9. 八维综合评级 >=70 才可受控部署；>=80 才可标记 A 级可用。
10. git status 无 D 状态、无意外文件变化，完整测试通过。

先输出：修改摘要、测试、回放指标、评级、已知风险、部署文件清单和回滚办法。只有用户明确确认后，才同步源代码到：
/Users/tonyyu/Library/Application Support/a-share-trading-watch

部署后只做一次非交易时段启动检查，不补生成历史报告、不手工推送飞书、不触发模拟交易。确认 launchd、health、数据库迁移和工作台正常后结束。
```

## 12. Codex 每阶段交付模板

```text
阶段：
评级变化：修改前 -> 修改后

发现：
- 事实、数据和文件位置。

实现：
- 修改文件和行为变化。

验证：
- 单元测试。
- 集成测试。
- 历史回放。
- 不变量检查。

验收指标：
- 通过项。
- 未通过项。

剩余风险：
- 仍需真实交易日前向验证的内容。

结论：
- PASS：允许进入下一阶段。
- FAIL：停留当前阶段继续修复。
```

## 13. 最终原则

可用系统不要求每天交易，但必须对每个重要目标形成连续、可解释的跟踪路径。任何“没有信号”都必须能区分：没有机会、尚未完成、数据不可用、没有预算、已经错过、不可成交或策略明确否决。只有这些状态都能被记录、回放和验证，零交易才可能是正确决策，而不是系统没有发挥作用。
