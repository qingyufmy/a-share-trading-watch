# GPT Pro 互动评审流程｜A股订盘项目

## 目的

GPT Pro 不作为执行者，作为“交易逻辑 + 工程落地 + 信号质量”的外部评审器。Codex 负责：

1. 整理上下文和问题。
2. 生成可复制给 GPT Pro 的评审提示词。
3. 接收用户粘贴回来的 GPT Pro 回复。
4. 把回复拆成可实施任务。
5. 修改代码、验证、同步 runtime。
6. 输出简短验收结果。

## 固定流程

### 1. 生成评审提示词

提示词必须包含：

- 项目目标。
- 当前架构。
- 已落地功能。
- 最近改动。
- 已知问题。
- P0/P1/P2 评审项。
- 强约束。
- 希望 GPT Pro 输出的格式。
- 必须要求公式、伪代码、字段、验收标准，不能只给泛泛建议。

当前主提示词：

- `GPT_PRO_IMPLEMENTATION_REVIEW_20260601.md`

### 2. 用户投给 GPT Pro

用户把提示词复制到 GPT Pro。

注意：

- 不要泄露飞书 webhook、cookie、token、账户信息。
- 不要求 GPT Pro 直接下投资建议。
- 只让 GPT Pro 审查系统规则和工程实现。

### 3. 用户贴回 GPT Pro 回复

Codex 收到回复后，不直接照单全收，先做二次筛选：

- 符合交易框架的保留。
- 违反 A 股规则的剔除。
- 只讲概念没有验收标准的降级。
- 会增加噪音或追涨的降级。
- 影响范围大的拆成独立版本。

### 4. 转成实施计划

按版本拆分：

```text
V13-1：P0 硬风控/软风控拆分
V13-2：P1 加仓确认收紧
V13-3：全市场机会池状态机化
V13-4：模拟盘滑点/成交约束
V13-5：盘后信号质量 schema
```

每个任务都要写：

- 文件范围。
- 改动内容。
- 不改什么。
- 验收命令。
- 验收结果。

### 5. 执行落地

执行顺序：

1. 小步改代码。
2. `py_compile` / `node --check`。
3. 本地 dry-run，不推飞书。
4. 工作台浏览器验证。
5. 同步 runtime。
6. 必要时强制刷新一轮报告。

### 6. 最终反馈格式

最终回复控制在高信号密度：

- 改了什么。
- 验证了什么。
- 还有什么没做。
- 下一步等 GPT Pro 回复或继续执行哪个 V13 子任务。

## 不做的事

- 不把 GPT Pro 当成事实源。
- 不让 GPT Pro 生成具体股票买卖建议。
- 不把 GPT Pro 的输出直接写入交易规则，必须经 Codex 二次审查。
- 不泄露任何 webhook、cookie、token。
- 不在没有验收标准的情况下改核心信号规则。
